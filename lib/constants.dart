import 'package:flutter/material.dart';

//colors that we use in our app
const kPrimaryColor = Color.fromARGB(255, 156, 60, 180);
const kTextColor = Color(0xFFA940BE);
const kBackgroundColor = Color.fromRGBO(210, 167, 214, 1);

const double kDefaultPadding = 20.0;
